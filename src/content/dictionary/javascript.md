---
term: "JavaScript"
category: "Frontend Engineering"
level: "Foundational"
---

The programming language of the web. Runs in browsers to make pages interactive. Also runs on servers (Node.js). The most widely used language in the world.

### Example

Click a button → form validates → data sends to server → page updates without reloading. All powered by JavaScript.
