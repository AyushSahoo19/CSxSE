---
term: "Hash Table / Hash Map"
category: "Data Structures"
level: "Foundational"
---

A data structure that maps keys to values using a hash function. Provides O(1) average-case lookup, insertion, and deletion.

### Example

A phone book indexed by name. dict['Alice'] = '555-1234'. The hash function converts 'Alice' into an array index instantly.
