---
term: "Proxy Pattern"
category: "OOP & Design Patterns"
level: "Advanced"
---

Provides a surrogate or placeholder for another object to control access. Types: virtual (lazy loading), protection (access control), remote (network proxy).

### Example

A virtual proxy for large images — shows a placeholder until the real image loads. The proxy has the same interface as the real image.
