---
term: "Hash Function"
category: "Data Structures"
level: "Intermediate"
---

A function that converts input of any size into a fixed-size value (hash). Good hash functions distribute values uniformly.

### Example

hash('Alice') → 7, hash('Bob') → 3. Maps names to specific 'slots'. A bad hash function puts everyone in slot 7 (collision).
