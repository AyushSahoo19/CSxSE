---
term: "OAuth 2.0"
category: "Networking & Web"
level: "Intermediate"
---

An authorization framework that lets third-party apps access user data without exposing passwords. Uses tokens instead of credentials.

### Example

'Sign in with Google' — the app never sees your Google password. Google gives the app a limited-access token. You can revoke access anytime.
