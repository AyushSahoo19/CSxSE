---
term: "WebSocket"
category: "Networking & Web"
level: "Intermediate"
---

A protocol providing full-duplex (two-way) communication over a single TCP connection. Server can push data to client without client asking.

### Example

Chat apps, live sports scores, stock tickers — the server pushes updates instantly. With HTTP, you'd have to keep asking 'any new messages?' (polling).
