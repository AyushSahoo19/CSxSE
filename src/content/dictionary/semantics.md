---
term: "Semantics"
category: "Fundamentals"
level: "Foundational"
---

The meaning of syntactically correct code. Syntax is grammar; semantics is what the sentence actually means.

### Example

x = x + 1 is syntactically valid AND semantically clear (increment x). x = x / 0 is syntactically valid but semantically a disaster.
