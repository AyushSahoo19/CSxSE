---
term: "Cron Job"
category: "Backend Engineering"
level: "Foundational"
---

A scheduled task that runs automatically at specified intervals. Named after the Unix cron scheduler.

### Example

0 2 * * * cleanup_old_sessions.sh — runs at 2 AM every day. 0 * * * * generate_reports.py — runs every hour on the hour.
