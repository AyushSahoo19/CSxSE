---
term: "DOM (Document Object Model)"
category: "Frontend Engineering"
level: "Foundational"
---

A tree representation of an HTML page that JavaScript can manipulate. Each HTML element is a node in the tree.

### Example

document.getElementById('title').textContent = 'New Title'; — JavaScript changes the page by modifying the DOM tree. The browser re-renders the change.
