---
term: "Containerization"
category: "Cloud & Scalability"
level: "Intermediate"
---

Packaging an application and its dependencies into a lightweight, portable container. More efficient than VMs — shares the host OS kernel.

### Example

Docker container: your Node.js app + node_modules + exact Node version. Runs identically on any machine with Docker. 10x lighter than a VM.
