---
term: "Space Complexity"
category: "Algorithms"
level: "Foundational"
---

The amount of memory an algorithm needs relative to input size. Includes input storage and auxiliary space.

### Example

Merge sort needs O(n) extra space for temporary arrays. In-place quicksort needs only O(log n) for the call stack.
