---
term: "System Call"
category: "Operating Systems"
level: "Intermediate"
---

The interface between user programs and the OS kernel. Programs request OS services (file I/O, networking, process creation) via system calls.

### Example

read(), write(), fork(), exec(), socket() — these aren't library functions but requests to the kernel to perform privileged operations on your behalf.
