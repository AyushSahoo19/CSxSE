---
term: "Immutability"
category: "Fundamentals"
level: "Intermediate"
---

The property of data that cannot be changed after creation. Instead of modifying, you create a new copy with changes.

### Example

Strings in Python/Java are immutable. 'hello'.upper() creates 'HELLO' — it doesn't change the original string.
