---
term: "AVL Tree"
category: "Data Structures"
level: "Advanced"
---

A self-balancing BST where the height difference between left and right subtrees is at most 1. Uses rotations to maintain balance.

### Example

If inserting a new word makes the tree lopsided, AVL automatically rotates nodes to keep it balanced — guarantees O(log n) always.
