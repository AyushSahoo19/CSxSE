---
term: "Type Inference"
category: "Fundamentals"
level: "Intermediate"
---

The compiler automatically deduces the type of a variable from its value, so you don't have to write it explicitly.

### Example

In TypeScript: let x = 42 — compiler knows x is a number without you saying 'let x: number = 42'.
