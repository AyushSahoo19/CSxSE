---
term: "Adapter Pattern"
category: "OOP & Design Patterns"
level: "Intermediate"
---

Converts the interface of a class into another interface that clients expect. Makes incompatible interfaces work together.

### Example

A power plug adapter lets a US plug work in a European socket. In code: wrapping a legacy XML API to expose a modern JSON interface.
