---
term: "Heap Sort"
category: "Algorithms"
level: "Intermediate"
---

Build a max-heap from the array, then repeatedly extract the maximum to build the sorted result. O(n log n) guaranteed, in-place.

### Example

Like a tournament bracket — the winner (max) is always at the top. Remove the winner, reorganize, new winner emerges. Repeat.
