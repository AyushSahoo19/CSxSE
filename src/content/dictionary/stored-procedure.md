---
term: "Stored Procedure"
category: "Databases & Storage"
level: "Intermediate"
---

Pre-compiled SQL code stored in the database that can be called by name. Runs on the database server, reducing network round trips.

### Example

A stored procedure 'transfer_funds(from, to, amount)' handles the entire transfer logic on the DB side — one call instead of multiple queries.
