---
term: "Linked List"
category: "Data Structures"
level: "Foundational"
---

A sequence of nodes where each node contains data and a pointer to the next node. Not stored contiguously in memory.

### Example

Like a treasure hunt — each clue (node) tells you where the next clue is. Insertion/deletion is O(1) at known positions but search is O(n).
