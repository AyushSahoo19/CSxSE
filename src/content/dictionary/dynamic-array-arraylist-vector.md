---
term: "Dynamic Array (ArrayList / Vector)"
category: "Data Structures"
level: "Foundational"
---

An array that can grow or shrink automatically. When full, it creates a larger internal array and copies elements over.

### Example

Python's list, Java's ArrayList. You keep appending items without worrying about size — it handles resizing internally.
