---
term: "Dijkstra's Algorithm"
category: "Algorithms"
level: "Intermediate"
---

Finds the shortest path from one node to all other nodes in a weighted graph with non-negative weights. Uses a priority queue. O((V+E) log V).

### Example

GPS navigation — finding the fastest route from your location to the airport, considering road distances/times as edge weights.
