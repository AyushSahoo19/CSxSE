---
term: "Return Value"
category: "Fundamentals"
level: "Foundational"
---

The output a function sends back to the code that called it. The function's 'answer'.

### Example

sum(3, 5) returns 8. The calling code can then use that 8: total = sum(3, 5) + tax.
