---
term: "Neural Network"
category: "AI / ML Foundations"
level: "Intermediate"
---

A computing model inspired by biological neurons. Layers of interconnected nodes (neurons) that learn to transform inputs into outputs through weighted connections.

### Example

Input layer (pixels of image) → hidden layers (learn edges, shapes, objects) → output layer (cat, dog, car). Each layer learns increasingly abstract features.
